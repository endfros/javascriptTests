let variableGlobal = 15;

function saludar(){
    let variableLocal = 10;
    console.log(variableLocal, variableGlobal)
}

saludar();

// Call stack o pila de ejecucion, genera una especie de variable por debajo, ciertos enlaces. 